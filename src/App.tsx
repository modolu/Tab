export default function App() {
  return (
    <main className="app-shell">
      <section className="intro-card" aria-labelledby="app-title">
        <p className="eyebrow">TAB</p>
        <h1 id="app-title">Shared costs, settled.</h1>
        <p className="intro-copy">
          A focused NIM collection flow for groups with one shared obligation.
        </p>
        <p className="status-note">Phase 0 shell ready for Phase 1.</p>
      </section>
    </main>
  )
}
